"""Embedding function helpers, re-exported from betterdb-semantic-cache.

``SemanticCache`` needs an ``embed_fn`` to turn text into a vector. Bring your
own, or import one of the provider-backed factories from a submodule here.
"""
