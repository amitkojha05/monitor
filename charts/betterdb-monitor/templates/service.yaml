apiVersion: v1
kind: Service
metadata:
  name: {{ include "betterdb-monitor.fullname" . }}
  labels:
    {{- include "betterdb-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "betterdb-monitor.selectorLabels" . | nindent 4 }}
